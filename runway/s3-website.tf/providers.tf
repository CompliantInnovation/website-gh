provider "aws" {
  region = var.region
  # default_tags {
  #   tags = {
  #     owner         = var.default_tags.owner
  #     project       = var.default_tags.project
  #     environment   = var.environment
  #   }
  # }
}